# salary-service-frontend environment variables
# In production this file is not needed — VITE_API_BASE_URL defaults to /api
# which works when the frontend is served by the salary-service Express server.
#
# Only needed if running the Vite dev server against a non-local backend:
VITE_API_BASE_URL=/api
